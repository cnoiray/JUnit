-- Fichier regroupant les differentes requetes SQL  --
-- ================================================ --

test.createtable=\
		CREATE TABLE   REGISTRATION (id INTEGER not NULL, first VARCHAR(255), \
		last VARCHAR(255), age INTEGER, PRIMARY KEY ( id ))